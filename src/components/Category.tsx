"use client";

import Image from "next/image";

export default function Category() {
  return (
    <div
      className="bg-yellow-500 p-4 rounded-xl shadow-md cursor-pointer"
      onClick={() => {}}
    >
      Title
    </div>
  );
}
